export const BAR_HEIGHT = 60;
export const SEARCH_HEIGHT = 52;

export const BACK_SIZE = 36;

export const BACK_INSET = (BAR_HEIGHT - BACK_SIZE) / 2;

export const ACTION_SIZE = 46;

export const ACTION_HEIGHT = ACTION_SIZE + BACK_INSET * 2;

export const ZONES_MAX = 250;

export const LIST_MAX = 350;

export interface Bounds {
    left: number;
    width: number;
}
